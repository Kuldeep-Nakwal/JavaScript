// ********************************* REPLACE ******************************************

// Searches a value in the string and returns a new string with the value replaced.

let str = "I love Coding.";

let newStr = str.replace("love", "do");

console.log(newStr);

// Replaces only first occurance of the element not all elements are replaced.

let newStr1 = str.replace("o", "a");

console.log(newStr1);
console.log(str);

let msg = "KuldeepKuldeep"

let newMsg = msg.replace("Kuldeep", "Nakwal");

console.log(newMsg);


// *********************************  REPEAT  ******************************************

// Returns a string with a number of copies of a string.

let item = "Mango ";

console.log(item.repeat(3));

console.log(item);


