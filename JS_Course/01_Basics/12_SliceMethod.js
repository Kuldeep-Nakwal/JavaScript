// *************************** SLICE METHODE *********************************

// Returns a part of original string as a new string.

let str = "ILoveCoding";

console.log(str.slice(1,5));
console.log(5);
// str.slice(-num) => str.slice(length - num);

console.log(str.slice(-6))  // str.slice(-6) = str.slice(11 - 6) = str.slice(5)

